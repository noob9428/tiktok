# Copy these into Streamlit Community Cloud → App settings → Secrets.
# Do not rename this example file to secrets.toml in a public repository.

OPENAI_API_KEY = "your-key-here"
OPENAI_MODEL = "gpt-5-mini"
